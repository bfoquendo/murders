We analyze US gun murders collected by the FBI.

download-data.R - downloads csv file to data directory

wrangle-data.R - creates a dreived dataset and saves as R objects in rda directory

analysis.R generates a plot and saves it to the Figs subdirectory 